# مشروع واجد (Wajad) - دليل المطور

## 🌟 نظرة عامة على المشروع

**واجد** هو تطبيق سياحي متكامل يهدف إلى توفير تجربة سفر سلسة ومتقدمة للمستخدمين. يجمع المشروع بين واجهة أمامية تفاعلية مبنية على React/Vite وواجهة خلفية قوية مبنية على Node.js/Express، مع استخدام قاعدة بيانات Supabase كحل شامل للبيانات والمصادقة.

تم تطوير هذا المشروع وتطبيق أحدث ممارسات هندسة البرمجيات والتصميم لضمان الأداء العالي، الأمان، وتجربة المستخدم (UX) الاستثنائية.

## 🛠️ التقنيات المستخدمة

| الفئة | التقنيات الرئيسية | الوصف |
| :--- | :--- | :--- |
| **الواجهة الأمامية (Frontend)** | React, Vite, CSS Modules | بناء واجهة مستخدم سريعة وفعالة. |
| **الواجهة الخلفية (Backend)** | Node.js, Express | بيئة تشغيل سريعة وإطار عمل مرن لبناء الـ API. |
| **قاعدة البيانات** | Supabase (PostgreSQL) | قاعدة بيانات علائقية قوية مع خدمات مصادقة في الوقت الفعلي. |
| **الأمان والمصادقة** | JWT (JSON Web Tokens) | آلية مصادقة قائمة على الرموز. |
| **التحقق من الصحة** | Joi | التحقق من صحة البيانات المدخلة في الواجهة الخلفية. |
| **الحركة والتفاعل** | Framer Motion | مكتبة متقدمة لإضافة تأثيرات حركية سلسة وجذابة. |

## 🚀 تحسينات هندسة البرمجيات المطبقة (العبقرية)

تم تطبيق مجموعة من التحسينات الهيكلية لرفع جودة الكود والأداء، مما جعل المشروع في مستوى احترافي متقدم:

### 1. الأمان وجودة الكود
*   **تطبيق آلية تحديث رمز الوصول (Token Refresh):** تم تعديل `authMiddleware.js` لتطبيق منطق تحديث رمز الوصول (Refresh Token) بشكل كامل، مما يعزز أمان الجلسات ويضمن استمراريتها.
*   **توحيد معالجة الأخطاء:** تم إنشاء فئة `ErrorResponse.js` ووسيط `errorMiddleware.js` لتوحيد استجابات الأخطاء في الواجهة الخلفية، مما يسهل على الواجهة الأمامية التعامل مع الأخطاء بشكل متسق.
*   **التحقق المتقدم من الصحة (Validation):** تم تطبيق مكتبة **Joi** على جميع مسارات الإدخال الرئيسية (المصادقة، نقاط الاهتمام، الحجز، الدفع) لضمان سلامة البيانات المدخلة.

### 2. تحسين الأداء (Performance)
*   **تجميع طلبات API (API Aggregation):** تم إنشاء مسار API واحد مخصص (`/api/v1/profile/data`) لجلب جميع بيانات صفحة الملف الشخصي في طلب واحد بدلاً من أربعة طلبات منفصلة، مما أدى إلى تقليل زمن التحميل بشكل كبير.
*   **البحث الآني المؤجل (Debounced Search):** تم إنشاء خطاف مخصص `useDebounce` في الواجهة الأمامية لتقليل عدد طلبات البحث المرسلة إلى الخادم أثناء كتابة المستخدم، مما يحسن أداء البحث.
*   **الترحيل (Pagination):** تم تطبيق دعم الترحيل على مسارات جلب الحجوزات لمنع بطء النظام عند التعامل مع كميات كبيرة من البيانات.

## 🗺️ تحليل وتطوير الصفحات الرئيسية

تم تطوير الصفحات التالية من حالة "قيد التطوير" إلى واجهات احترافية وعبقرية، مع التركيز على تجربة المستخدم المتقدمة (UX) وتطبيق تأثيرات `Framer Motion`:

| الصفحة | الهدف الرئيسي من التطوير | الميزات المطبقة |
| :--- | :--- | :--- |
| **الملف الشخصي (`ProfilePage.jsx`)** | واجهة احترافية لإدارة الحساب | دمج بطاقة نقاط الولاء (Loyalty Card)، استخدام مسار API مجمع، وتأثيرات حركية متقدمة. |
| **الحجز (`BookingPage.jsx`)** | عملية حجز سلسة ومتقدمة | تحويل إلى **نموذج متعدد الخطوات (Multi-step Form)** مع شريط تقدم واضح وتأثيرات انتقال سلسة. |
| **الاستكشاف (`ExplorationPage.jsx`)** | بحث فعال وعرض فوري للنتائج | تطبيق خطاف `useDebounce` للبحث الآني، وعرض نتائج نقاط الاهتمام (POI) بشكل تفاعلي. |
| **الإقامة (`AccommodationPage.jsx`)** | بحث وتصفية متقدمة للإقامة | واجهة بحث متقدمة (التاريخ، السعر، التقييم) مع مكون `FilterBar` مخصص. |
| **المطاعم (`DiningPage.jsx`)** | بحث وتصفية متقدمة للمطاعم | واجهة بحث متقدمة (نوع المطبخ، التقييم، السعر) مع مكون `DiningFilterBar` مخصص. |
| **النقل (`TransportPage.jsx`)** | بحث ومقارنة لخدمات النقل | واجهة بحث متقدمة (النوع، السعة، السعر) مع مكون `TransportFilterBar` مخصص. |
| **الأمان (`SafetyPage.jsx`)** | معلومات طوارئ تفاعلية | دمج **خريطة طوارئ تفاعلية** (Emergency Map) لجلب وعرض مواقع المستشفيات والشرطة القريبة، وعرض أرقام الطوارئ المحلية. |

## 📂 هيكلة المجلدات الرئيسية

لضمان سهولة الصيانة وقابلية التوسع، تم تنظيم المشروع على النحو التالي:

| المجلد | الوصف |
| :--- | :--- |
| `controllers/` | وحدات التحكم في الواجهة الخلفية التي تعالج منطق الأعمال. |
| `middleware/` | وظائف وسيطة (Middleware) مثل المصادقة، التحقق من الصحة، ومعالجة الأخطاء. |
| `routes/` | تعريف مسارات API. |
| `services/` | وحدات برمجية منفصلة للتعامل مع الخدمات الخارجية والمنطق المعقد (GIS، الولاء). |
| `validation/` | ملفات التحقق من الصحة (Validation Schemas) باستخدام Joi. |
| `utils/` | أدوات مساعدة عامة (مثل `ErrorResponse.js`). |
| `frontend/src/pages/` | ملفات الصفحات الرئيسية للواجهة الأمامية (React Components). |
| `frontend/src/components/` | المكونات القابلة لإعادة الاستخدام (مثل `LoyaltyCard`, `FilterBar`). |
| `frontend/src/hooks/` | الخطافات المخصصة (Custom Hooks) مثل `useDebounce` و `useBookingForm`. |

## ⚙️ دليل الإعداد والتشغيل

### المتطلبات الأساسية
*   Node.js (يفضل الإصدار 18+)
*   pnpm (أو npm/yarn)
*   قاعدة بيانات Supabase (أو PostgreSQL محلية)

### 1. تثبيت الاعتماديات

في المجلد الرئيسي للمشروع:

```bash
# تثبيت اعتماديات الواجهة الخلفية
pnpm install

# تثبيت اعتماديات الواجهة الأمامية
cd frontend
pnpm install
cd ..
```

### 2. إعداد المتغيرات البيئية

قم بإنشاء ملف `.env` في المجلد الرئيسي للمشروع وأضف متغيرات Supabase:

```
# .env
SUPABASE_URL=your_supabase_url
SUPABASE_KEY=your_supabase_anon_key
JWT_SECRET=your_secret_key_for_jwt
REFRESH_TOKEN_SECRET=your_secret_key_for_refresh_token
```

### 3. تشغيل المشروع

```bash
# تشغيل الواجهة الخلفية (الخادم)
pnpm start

# تشغيل الواجهة الأمامية (التطبيق)
cd frontend
pnpm dev
```

سيتم تشغيل الواجهة الخلفية على المنفذ `5000` (افتراضياً) والواجهة الأمامية على المنفذ `5173` (افتراضياً).

## 🤝 المساهمة

نرحب بالمساهمات في هذا المشروع. يرجى اتباع الخطوات التالية:
1.  قم بعمل تفرع (Fork) للمستودع.
2.  أنشئ فرعاً جديداً للميزة الخاصة بك (`git checkout -b feature/AmazingFeature`).
3.  قم بتثبيت التغييرات الخاصة بك (`git commit -m 'feat: إضافة ميزة رائعة'`).
4.  ادفع إلى الفرع (`git push origin feature/AmazingFeature`).
5.  افتح طلب سحب (Pull Request).

---
**تم تطوير هذا المشروع بواسطة Manus AI**
**© 2025**
