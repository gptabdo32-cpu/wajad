# 🌴 منصة الخمس السياحية - دليل المطور

## ✨ نظرة عامة

**منصة الخمس السياحية** الهدف: تطبيق متكامل يهدف إلى توفير تجربة سفر سلسة ومتقدمة للمستخدمين المهتمين بالسياحة في منطقة الخمس، ليبيا. يركز المشروع على دمج واجهة مستخدم تفاعلية مع خدمات خلفية قوية ومؤمنة.

التقنيات الأساسية: المشروع هو تطبيق Full-Stack يستخدم أحدث التقنيات في تطوير الويب


تم تطوير التطبيق وتطبيق أحدث ممارسات هندسة البرمجيات والتصميم لضمان الأداء العالي، الأمان، وتجربة المستخدم (UX) الاستثنائية.

## ⚙️ التقنيات الأساسية

| الفئة | التقنيات الرئيسية | الوصف |
| :--- | :--- | :--- |
| **الواجهة الأمامية** | React, Vite, CSS Modules | بناء واجهة مستخدم سريعة وفعالة. |
| **الواجهة الخلفية** | Node.js, Express | بيئة تشغيل سريعة وإطار عمل مرن لبناء الـ API. |
| **قاعدة البيانات** | Supabase (PostgreSQL) | قاعدة بيانات علائقية قوية مع خدمات مصادقة في الوقت الفعلي. |
| **الأمان** | JWT (JSON Web Tokens) | آلية مصادقة قائمة على الرموز. |
| **التحقق** | Joi | التحقق من صحة البيانات المدخلة في الواجهة الخلفية. |
| **الحركة** | Framer Motion | مكتبة متقدمة لإضافة تأثيرات حركية سلسة وجذابة. |

## 🚀 تحسينات هندسة البرمجيات (العبقرية)

تم تطبيق مجموعة من التحسينات الهيكلية لرفع جودة الكود والأداء، مما جعل التطبيق في مستوى احترافي متقدم:

### 1. 🛡️ الأمان وجودة الكود
*   **تحديث رمز الوصول:** تم تعديل `authMiddleware.js` لتطبيق منطق تحديث رمز الوصول (Refresh Token) بشكل كامل، مما يعزز أمان الجلسات ويضمن استمراريتها.
*   **توحيد معالجة الأخطاء:** تم إنشاء فئة `ErrorResponse.js` ووسيط `errorMiddleware.js` لتوحيد استجابات الأخطاء في الواجهة الخلفية، مما يسهل على الواجهة الأمامية التعامل مع الأخطاء.
*   **التحقق المتقدم:** تم تطبيق مكتبة **Joi** على جميع مسارات الإدخال الرئيسية لضمان سلامة البيانات.

### 2. ⚡ تحسين الأداء
*   **تجميع طلبات API:** تم إنشاء مسار API واحد مخصص (`/api/v1/profile/data`) لجلب جميع بيانات صفحة الملف الشخصي في طلب واحد بدلاً من أربعة، مما قلل زمن التحميل بشكل كبير.
*   **البحث الآني المؤجل:** تم إنشاء خطاف مخصص `useDebounce` في الواجهة الأمامية لتقليل عدد طلبات البحث المرسلة إلى الخادم، مما يحسن أداء البحث.
*   **الترحيل:** تم تطبيق دعم الترحيل على مسارات جلب الحجوزات لمنع بطء النظام عند التعامل مع كميات كبيرة من البيانات.

## 🌐 تحليل وتطوير الصفحات الرئيسية

الوظيفة الأساسية للمنصة هي تسهيل تجربة المستخدم السياحية من خلال مجموعة من الخدمات التفاعلية. تم تطوير الصفحات والميزات التالية بشكل احترافيFramer Motion`:

| الصفحة | الهدف الرئيسي من التطوير | الميزات المطبقة |
| :--- | :--- | :--- |
| **الملف الشخصي** | واجهة احترافية لإدارة الحساب | دمج بطاقة نقاط الولاء، استخدام مسار API مجمع، وتأثيرات حركية متقدمة. |
| **الحجز** | عملية حجز سلسة ومتقدمة | تحويل إلى **نموذج متعدد الخطوات** مع شريط تقدم واضح وتأثيرات انتقال سلسة. |
| **الاستكشاف** | بحث فعال وعرض فوري للنتائج | تطبيق خطاف `useDebounce` للبحث الآني، وعرض نتائج نقاط الاهتمام (POI) بشكل تفاعلي. |
| **الإقامة** | بحث وتصفية متقدمة للإقامة | واجهة بحث متقدمة (التاريخ، السعر، التقييم) مع مكون `FilterBar` مخصص. |
| **المطاعم** | بحث وتصفية متقدمة للمطاعم | واجهة بحث متقدمة (نوع المطبخ، التقييم، السعر) مع مكون `DiningFilterBar` مخصص. |
| **النقل** | بحث ومقارنة لخدمات النقل | واجهة بحث متقدمة (النوع، السعة، السعر) مع مكون `TransportFilterBar` مخصص. |
| **الأمان** | معلومات طوارئ تفاعلية | دمج **خريطة طوارئ تفاعلية** لجلب وعرض مواقع المستشفيات والشرطة القريبة، وعرض أرقام الطوارئ المحلية. |

## 🗂️ هيكلة المجلدات

لضمان سهولة الصيانة وقابلية التوسع، تم بناء المشروع وفقًا لمبادئ هندسة البرمجيات المتقدمة لضمان قابلية الصيانة، الأداء العالي، والأمان.تم تنظيم التطبيق على النحو التالي:

| المجلد | الوصف |
| :--- | :--- |
| `controllers/` | وحدات التحكم في الواجهة الخلفية التي تعالج منطق الأعمال. |
| `middleware/` | وظائف وسيطة مثل المصادقة، التحقق، ومعالجة الأخطاء. |
| `routes/` | تعريف مسارات API. |
| `services/` | وحدات برمجية منفصلة للتعامل مع الخدمات الخارجية والمنطق المعقد (GIS، الولاء). |
| `validation/` | ملفات التحقق من الصحة (Validation Schemas) باستخدام Joi. |
| `utils/` | أدوات مساعدة عامة (مثل `ErrorResponse.js`). |
| `frontend/src/pages/` | ملفات الصفحات الرئيسية للواجهة الأمامية (React Components). |
| `frontend/src/components/` | المكونات القابلة لإعادة الاستخدام (مثل `LoyaltyCard`, `FilterBar`). |
| `frontend/src/hooks/` | الخطافات المخصصة (Custom Hooks) مثل `useDebounce` و `useBookingForm`. |

## 💻 دليل الإعداد والتشغيل

### المتطلبات الأساسية
*   Node.js (يفضل الإصدار 18+)
*   pnpm (أو npm/yarn)
*   قاعدة بيانات Supabase

### 1. تثبيت الاعتماديات

في المجلد الرئيسي للتطبيق:

```bash
# تثبيت اعتماديات الواجهة الخلفية
pnpm install

# تثبيت اعتماديات الواجهة الأمامية
cd frontend
pnpm install
cd ..
```

### 2. إعداد المتغيرات البيئية

قم بإنشاء ملف `.env` في المجلد الرئيسي للتطبيق وأضف متغيرات Supabase:

```
# .env
SUPABASE_URL=your_supabase_url
SUPABASE_KEY=your_supabase_anon_key
JWT_SECRET=your_secret_key_for_jwt
REFRESH_TOKEN_SECRET=your_secret_key_for_refresh_token
```

### 3. تشغيل التطبيق

```bash
# تشغيل الواجهة الخلفية (الخادم)
pnpm start

# تشغيل الواجهة الأمامية (التطبيق)
cd frontend
pnpm dev
```

سيتم تشغيل الواجهة الخلفية على المنفذ `5000` (افتراضياً) والواجهة الأمامية على المنفذ `5173` (افتراضياً).

---
**تطوير التطبيق بواسطة Wajad**
**© 2025**
